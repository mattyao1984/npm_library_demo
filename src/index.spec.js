var hearthstoneSkills = require('./index');
var expect = require('chai').expect;

describe('hearthstone-skills', function() {
  it('should just work.', function() {
    expect(true).to.be.true;
  });
})
